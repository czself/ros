# Coder subtask: runtime lifecycle
Part of task002 approved plan. Read skill/context/architecture_repair/plan/architecture_review.
Implement scripts/runtime_control.py inside Docker (stop project nodes and launch processes safely without shell pkill self-matches, stop velocities, reset fixed chassis pose only on explicit startup, verify fresh clock/scan/services; CLI stop/reset/health). Host scripts runtime_common.sh, start_sim.sh, start_navigation.sh, start_slam_mapping.sh, start_autonomous_mapping.sh, view_rviz.sh/view_autonomous_mapping.sh.
Root owns model_state_odom.py, navigation configs/launch, world, RViz configs. Do not edit those or run any mutating runtime command until root coordinates. You may static-check owned files.
Launch odom as separate docker exec -d python3 /root/model_state_odom.py; TF provided navigation/robot.launch included in navigation and mapping launches. Launch /root/navigation/mapping_exploration.launch explore:=false for manual mapping; true starts move_base but root later adds frontier controller. navigation launch args map_file,initial_x,initial_y,initial_yaw (axle map pose); default chassis world birth=(4.0833,-4.0833,yaw1.5708), axle offset=.125m. Later map manifest will provide map_to_odom for saved initial pose.
Ensure node/launch ownership cleanup, stale parameter cleanup, action readiness health; start_sim explicit --restart handles old Gazebo and ROS project processes, copy world and models to container, only initialize/restart as needed. Never delete user maps. Default navigation map should be competition_slam_verified, fail clearly if absent rather than silently truth fallback. Caller may supply explicit diagnostic map. Input must be safely quoted. Original display :1.
Write runtime_summary.md under task002 with files/static results, report to root.
